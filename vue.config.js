
module.exports = {

  publicPath: '/maps',
  outputDir: 'dist/maps',
  assetsDir: 'static',

/*
  devServer: {
    proxy: {
      '/api': {
        target: 'http://localhost:8088', // this configuration needs to correspond to the backends' server port
        ws: true,
        changeOrigin: true
      }
    }
  }
*/

};
